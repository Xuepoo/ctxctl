name: "ctxctl"
arch: "amd64"
platform: "linux"
# Version is synced from Cargo.toml by the release workflow.
version: "0.1.0"
section: "default"
priority: "optional"
maintainer: "fuyu"
description: "CLI-first, stateless context layer for AI coding agents (optional MCP adapter)"
license: "MIT"
contents:
  - src: ./ctxctl-linux-x86_64
    dst: /usr/bin/ctxctl
