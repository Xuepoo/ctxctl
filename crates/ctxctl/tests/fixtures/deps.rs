//! Fixture exercising the deps command (rust).

use crate::lib::helper;
use serde::Deserialize;
use std::collections::HashMap;

mod frontend;

pub fn main() {
    let _: HashMap<String, String> = HashMap::new();
}
